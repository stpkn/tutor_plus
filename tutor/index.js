// Точка входа в React приложение
// Получаем корневой элемент из HTML
const root = ReactDOM.createRoot(document.getElementById('root'));

// Рендерим наш компонент App в корневой элемент
root.render(<App />);